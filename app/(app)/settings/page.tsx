import { PageStub } from "@/components/ui";

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      description="Account, default assumptions per market, currency and formatting preferences."
    />
  );
}
